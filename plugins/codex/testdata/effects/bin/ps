#!/usr/bin/env sh
# The pane's child reports itself as the headless worker, which matches both
# the interactive codex effect's "codex|codex-*" pattern and exec_runtime's
# "codex-exec*" pattern, so this one stand-in serves both effects' searches.
plect-effect-record ps "$@"
printf '%s\n' "codex-exec-worker"
