#!/usr/bin/env sh
# A process distinct from the pane's own root (PLECT_EFFECT_PID), so a
# scenario can prove a kill-on-failure path left the pane alone.
plect-effect-record pgrep "$@"
printf '%s\n' "$PLECT_EFFECT_WORKER_PID"
