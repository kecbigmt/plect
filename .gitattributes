# The committed Vite production build embedded by app/internal/webui/webapp
# (see that package's embed.go for why it is committed rather than built at
# install time). CI regenerates and diffs it; reviewers should read
# web/app/src/ instead.
app/internal/webui/webapp/dist/** linguist-generated

# oapi-codegen's committed output (web-api-contract's CI job regenerates and
# diffs it against main.tsp's schema); reviewers should read web/api/ instead.
app/internal/webapi/generated/** linguist-generated
